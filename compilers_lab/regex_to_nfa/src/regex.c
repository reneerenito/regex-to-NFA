#include "regex.h"
#include <stdlib.h>

regex parse_regex(const char *str)
{
    /* TODO: convertir la regex infija a postfija (shunting-yard) */
    (void)str;
    regex r = {NULL, 0};
    return r;
}
